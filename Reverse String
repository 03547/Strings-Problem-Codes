
 string reverseWord(string str)
    
    {
        
	    int s=0;
	    int n=str.size();
	    int e=n-1;
	    while(s<=e){
	       swap(str[s],str[e]);
	            s++;
	            e--;
	        }
	       return str;
	        
	    
        
        
    }
